# macOS code signing / notarization (all optional).
# Leave everything unset for local ad-hoc signing (identity '-').

# Set to your "Developer ID Application: Your Name (TEAMID)" to sign for distribution.
APPLE_IDENTITY=

# Set ALL THREE to enable notarization during `npm run make`:
APPLE_ID=
APPLE_PASSWORD=
APPLE_TEAM_ID=
